main:
  logfile: bender.log
  loglevel: debug
  channellogs:
    channels: ["#cafeen"]
    root: "channellogs"

# identity is the identity of the bot. Can be overridden in the `servers` section on a per-server basis
identity:
  nick: "Bender1"
  name: "Bender Bending Rodriguez"
  modestring: ""

# servers section defines servers to connect to, and per-server settings
servers:
  farnsworth.parabox.dk:
    network: "cafeen"
    port: 6697
    ssl: true
    sslskipverify: false
    password: folkerusletaaber
    channels: ["#bendertest", "#testbot"]
    ignore: ["flexo"]
